select *from(select E.EMPLOYEE_ID,E.FIRST_NAME,E.LAST_NAME,D.DEPARTMENT_NAME from EMPLOYEES E FULL OUTER JOIN DEPARTMENTS D on E.DEPARTMENT_ID=D.DEPARTMENT_ID where E.MANAGER_ID<>(select MANAGER_ID from DEPARTMENTS where DEPARTMENT_NAME='IT'));
